# 팀원 시작 가이드

Claude Code로 MeetFlow 작업을 시작하는 방법입니다. 순서대로 따라오시면 됩니다.

---

## 0. 준비물 확인

터미널(Windows는 **PowerShell**, Mac은 **터미널**)을 열고 하나씩 쳐보세요.

```bash
git --version
```
```bash
claude --version
```

둘 다 버전 숫자가 나오면 준비 끝입니다.

**안 나온다면**
- `git` 없음 → https://git-scm.com 에서 설치
- `claude` 없음 → 설치는 했는데 안 잡히는 경우가 많아요. **터미널을 완전히 껐다가 새로 열어보세요.** 그래도 안 되면 단톡방에 말해주세요.

---

## 1. 코드 받기

터미널에서 코드를 받을 폴더로 이동한 뒤:

```bash
git clone https://github.com/suming030/meetflow.git
```
```bash
cd meetflow
```

> **`cd meetflow`를 꼭 해주세요.** 이 폴더 안에 들어가 있어야 다음 단계가 제대로 동작합니다.

받아지지 않으면 저장소 초대를 아직 안 받은 것일 수 있어요. 수민에게 GitHub 아이디를 알려주세요.

---

## 2. 내 브랜치 만들기

**main 브랜치에서 직접 작업하면 안 됩니다.** 항상 최신 main을 받고 나서 내 브랜치를 파세요.

```bash
git checkout main
```
```bash
git pull origin main
```
```bash
git checkout -b feature/기능명
```

`기능명`은 본인이 할 일로 바꾸세요. 예시:
- 디자인 A → `feature/landing-design`
- 디자인 B → `feature/dashboard-design`
- 기능 D → `feature/google-docs`

---

## 3. Claude Code 실행

**반드시 `meetflow` 폴더 안에서** 실행하세요.

```bash
claude
```

이 폴더에는 `CLAUDE.md`라는 파일이 있는데, Claude가 자동으로 읽습니다.
거기에 "이 파일은 건드리지 마라", "이렇게 바꾸면 다 깨진다" 같은 규칙이 적혀 있어요.
**다른 폴더에서 실행하면 이걸 못 읽어서 엉뚱하게 고칩니다.**

---

## 4. 첫 대화

이렇게 물어보고 시작하세요.

```
CLAUDE.md 읽고 이 프로젝트 구조랑 내가 지켜야 할 규칙 알려줘.
나는 디자인 A 담당이야.
```

(본인 담당으로 바꿔서 — 디자인 A / 디자인 B / 기능 D)

내가 고쳐도 되는 파일과 하면 안 되는 것들을 정리해줄 겁니다.

### 담당별 첫 작업 예시

**디자인 A**
```
랜딩 페이지 히어로 안에 있는 제품 미리보기 목업이 실제 화면과 달라.
지금 대시보드 탭이 개요/Action Items/담당자별/마일스톤/Task Flow/브리핑인데
목업에는 마일스톤이 빠져 있고 이름도 달라. 실제와 맞게 고쳐줘.
내 담당 파일은 css/base.css, css/landing.css, index.html의 랜딩 영역이야.
```

**디자인 B**
```
회의 타임라인 화면을 새로 만들어줘. 이 서비스에서 제일 중요한 화면이야.
1차 → 2차 → 3차 회의가 세로로 이어지고, 각 회의에서 뭐가 결정됐는지와
지난 회의에서 이월된 일이 뭔지 보이는 화면이야.
전역 변수 history에 회의가 최신순으로 들어 있어. 먼저 구조를 확인하고 시작해줘.
내 담당 파일은 css/app.css, js/dashboard.js야.
```

**기능 D**
```
js/google.js에 이미 캘린더 등록이랑 Docs 내보내기가 들어가 있어.
먼저 그 코드를 읽고 지금 어디까지 됐는지 알려줘.
내 담당 파일은 js/google.js야.
```

---

## 5. 작업 중 알아둘 것

### Claude가 물어보는 것들

파일을 고치거나 명령어를 실행할 때 **허용할지 물어봅니다.**
- 파일 **읽기**나 검색 → 그냥 허용해도 됩니다
- 파일 **수정** → 어느 파일을 고치는지 보고 허용하세요.
  **내 담당이 아닌 파일을 고치려 하면 거절하고** "그 파일은 다른 팀원 담당이야"라고 말해주세요.

### 확인하는 방법

```bash
python -m http.server 8000
```

브라우저에서 **http://localhost:8000** 을 엽니다.

> ⚠️ `index.html`을 더블클릭해서 열면 **구글 로그인이 안 됩니다.** 꼭 위 명령어로 띄우세요.
> (`python`이 없으면 VS Code에 **Live Server** 확장을 깔고 `index.html` 우클릭 → Open with Live Server)

### 화면이 안 바뀔 때

코드를 고쳤는데 브라우저가 그대로면 **캐시**입니다. 자주 겪게 돼요.

**`Ctrl` + `Shift` + `R`** (Mac은 `Cmd` + `Shift` + `R`)

### AI 기능이 안 될 때

콘솔(F12)에 이런 에러가 나오면:
```
401 App Check token is invalid
```
**코드 문제가 아닙니다.** Firebase 설정 문제이니 고치려 하지 말고 단톡방에 알려주세요.

---

## 6. 작업 끝나면 올리기

```bash
git add .
```
```bash
git commit -m "무엇을 했는지 한 줄로"
```
```bash
git push origin feature/기능명
```

그다음 GitHub에 들어가면 노란 띠로 **"Compare & pull request"** 버튼이 보입니다.
누르고 → 무엇을 했는지 적고 → **Create pull request**.

수민이 확인하고 합쳐줍니다. **main에 직접 push하지 마세요.**

---

## 7. 다른 사람 코드가 합쳐진 뒤

누군가의 PR이 머지됐다는 얘기가 나오면, 내 작업에 최신 코드를 받아오세요.

```bash
git checkout main
```
```bash
git pull origin main
```
```bash
git checkout feature/기능명
```
```bash
git merge main
```

> 오래 묵힐수록 충돌이 커집니다. **하루 한 번**은 받아오는 걸 권해요.

충돌이 났는데 어떻게 해야 할지 모르겠으면, Claude에게 그대로 물어보세요:
```
git merge 하다가 충돌났어. 어떻게 해결해야 해?
```

---

## 막히면

1. Claude에게 그대로 물어보기 — 에러 메시지를 **복사해서 붙여넣으면** 제일 정확합니다
2. 그래도 안 되면 단톡방에 **에러 메시지 캡처**와 함께 올려주세요

"이거 물어봐도 되나?" 싶은 건 그냥 물어보세요. 잘못 고쳐서 되돌리는 게 더 오래 걸려요.
